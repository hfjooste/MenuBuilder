namespace ThirdPixelGames.MenuBuilder
{
    /// <summary>
    /// The type of menus that the library supports
    /// </summary>
    public enum MenuStyle
    {
        Vertical, // A vertical styled menu
        Horizontal, // A horizontal styled menu
        Tabs // A tab styled menu
    }
}